{
  "protocolVersion": "020",
  "description": "Papers-owned window helper resource. Protocol, session tokens, identity revalidation, capacity limits and native behavior are locked to the accepted Assignment 013 (013R..013R5F) probe snapshot, extended by the reviewed Assignment 016 additions (task-worthy list filter, hover, apply clamping), Assignment 019G thumbnails, atomic toggle, Assignment 018 window-class identity, Assignment 020 capability-restricted termination, and Assignment 021 stable persisted window identities derived from HWND, PID, process creation ticks and class. Session tokens are keyed by HWND + PID + live process creation time + window class, and every token use - observe, every mutation, and the uncloak visibility restore - revalidates the live process creation time in the same request handler, failing closed when it changed or cannot be read. A main-process-internal `reveal-instance` request reveals a window hidden by an earlier Peek by its stable instance identity alone: it recomputes the identity from a RAW top-level enumeration (hidden windows included), never consults a session token, reveals nothing on an ambiguous match, and has no IPC route. Stable IDs are reconciliation keys only; every mutation still requires an opaque session capability. The hashes below must equal the compiled expected hashes in src/main/windows/windowHelperResource.ts and the actual resource bytes; deliberate helper edits require an explicit code+manifest update.",
  "executable": "powershell.exe",
  "spawnArguments": [
    "-NoProfile",
    "-NonInteractive",
    "-File"
  ],
  "files": [
    "window-helper.ps1",
    "window-capability.ps1"
  ],
  "hashes": {
    "window-helper.ps1": "0d72f25977954695414190f51b05acc12e671697d0805527a1c72bf26abfd54d",
    "window-capability.ps1": "21a611a11560c1660dd2fffd39a2d2e637db9fe9f6adb132e7ff6b2433d7df8a"
  }
}
