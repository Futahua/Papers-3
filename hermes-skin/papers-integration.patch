diff --git a/apps/desktop/electron/main.ts b/apps/desktop/electron/main.ts
index fbf80f6bc9..14bf991e17 100644
--- a/apps/desktop/electron/main.ts
+++ b/apps/desktop/electron/main.ts
@@ -13924,6 +13924,166 @@ function closeQuickEntryWindow() {
   quickEntryWindow = null
 }
 
+// Papers companion-window seam. Inert unless Papers supplies an authenticated
+// loopback endpoint when it launches this Desktop.
+type PapersDockPayload = {
+  phase: string
+  bounds?: Electron.Rectangle
+  controlPort?: number | null
+  maximized?: boolean
+  minimized?: boolean
+  focused?: boolean
+  display?: number | null
+}
+
+let papersDockPost: ((payload: PapersDockPayload) => Promise<boolean>) | null = null
+
+function installPapersDockReporter(win: BrowserWindow) {
+  const dockUrl = process.env.HERMES_DESKTOP_PAPERS_DOCK_URL
+  const dockToken = process.env.HERMES_DESKTOP_PAPERS_DOCK_TOKEN
+  if (!dockUrl || !dockToken || win.isDestroyed()) return
+
+  let endpoint: URL
+  try {
+    endpoint = new URL(dockUrl)
+  } catch {
+    return
+  }
+
+  const post = (payload: PapersDockPayload): Promise<boolean> =>
+    new Promise(resolvePost => {
+      try {
+        const body = Buffer.from(JSON.stringify(payload))
+        const req = http.request(
+          {
+            hostname: endpoint.hostname,
+            port: endpoint.port,
+            path: endpoint.pathname || '/',
+            method: 'POST',
+            headers: {
+              'content-type': 'application/json',
+              'content-length': body.length,
+              'x-papers-dock-token': dockToken
+            },
+            timeout: 1000
+          },
+          res => {
+            res.resume()
+            res.once('end', () => resolvePost(Boolean(res.statusCode && res.statusCode >= 200 && res.statusCode < 300)))
+          }
+        )
+        req.once('error', () => resolvePost(false))
+        req.once('timeout', () => {
+          req.destroy()
+          resolvePost(false)
+        })
+        req.end(body)
+      } catch {
+        resolvePost(false)
+      }
+    })
+
+  papersDockPost = post
+  const report = (phase: string) => {
+    if (win.isDestroyed()) return
+    const bounds = win.getBounds()
+    void post({
+      phase,
+      bounds,
+      maximized: win.isMaximized(),
+      minimized: win.isMinimized(),
+      focused: win.isFocused(),
+      display: (() => {
+        try {
+          return screen.getDisplayMatching(bounds).id
+        } catch {
+          return null
+        }
+      })()
+    })
+  }
+
+  let moveTimer: NodeJS.Timeout | null = null
+  const scheduleMove = () => {
+    if (moveTimer) return
+    moveTimer = setTimeout(() => {
+      moveTimer = null
+      report('move')
+    }, 16)
+  }
+  win.on('move', scheduleMove)
+  win.on('resize', scheduleMove)
+  for (const event of ['moved', 'resized', 'maximize', 'unmaximize', 'restore', 'minimize', 'focus', 'show'] as const) {
+    win.on(event, () => report(event === 'focus' ? 'focus' : 'settle'))
+  }
+
+  const server = http.createServer((req, res) => {
+    if (req.method !== 'POST' || req.headers['x-papers-dock-token'] !== dockToken) {
+      res.writeHead(req.method === 'POST' ? 401 : 405)
+      res.end()
+      return
+    }
+    let raw = ''
+    req.on('data', chunk => {
+      raw += chunk
+      if (raw.length > 4096) req.destroy()
+    })
+    req.on('end', () => {
+      try {
+        const command = JSON.parse(raw || '{}')
+        if (win.isDestroyed()) {
+          res.writeHead(410)
+          res.end()
+          return
+        }
+        if (command.op === 'setBounds' && command.bounds) {
+          const next = command.bounds
+          const valid = [next.x, next.y, next.width, next.height].every(Number.isFinite)
+          if (!valid || next.width < 320 || next.height < 300) throw new Error('Invalid window bounds')
+          if (win.isMaximized()) win.unmaximize()
+          if (win.isMinimized()) win.restore()
+          win.setBounds({
+            x: Math.round(next.x),
+            y: Math.round(next.y),
+            width: Math.round(next.width),
+            height: Math.round(next.height)
+          })
+          if (command.focus) win.focus()
+          if (command.raise) win.moveTop()
+        } else if (command.op === 'focus') {
+          if (win.isMinimized()) win.restore()
+          win.show()
+          win.focus()
+        } else if (command.op === 'minimize') {
+          win.minimize()
+        } else if (command.op === 'raise') {
+          win.moveTop()
+        }
+        res.writeHead(200, { 'content-type': 'application/json' })
+        res.end(JSON.stringify({ ok: true, bounds: win.getBounds() }))
+      } catch (error) {
+        res.writeHead(400, { 'content-type': 'application/json' })
+        res.end(JSON.stringify({ ok: false, error: String(error) }))
+      }
+    })
+  })
+  server.on('error', () => {})
+  server.listen(0, '127.0.0.1', () => {
+    const address = server.address()
+    void post({
+      phase: 'hello',
+      controlPort: address && typeof address === 'object' ? address.port : null,
+      bounds: win.getBounds()
+    })
+  })
+  win.once('closed', () => {
+    papersDockPost = null
+    void post({ phase: 'closed' })
+    server.close()
+  })
+  report('settle')
+}
+
 function createWindow() {
   const icon = getAppIconPath()
   const savedWindowState = readWindowState()
@@ -14047,6 +14207,7 @@ function createWindow() {
 
   streamThrottle.register(mainWindow)
   wireCommonWindowHandlers(mainWindow, zoomWiringForWindowKind('chat'))
+  installPapersDockReporter(mainWindow)
 
   // Per-window renderer lifecycle diagnostics + recovery (#81290). The reload
   // policy (crashed/oom → bounded reload via the shared rolling budget, then
@@ -16868,6 +17029,24 @@ ipcMain.handle('hermes:updates:apply', async (_event, payload) =>
   }))
 )
 
+ipcMain.handle('hermes:papers:update', async () => {
+  if (!papersDockPost) {
+    return { ok: false, error: 'papers-unavailable', message: 'Papers is not connected.' }
+  }
+  const delivered = await papersDockPost({ phase: 'update-request' })
+  if (!delivered) {
+    return { ok: false, error: 'papers-unavailable', message: 'Papers did not accept the update request.' }
+  }
+  emitUpdateProgress({
+    stage: 'restart',
+    message: 'Papers is closing Hermes, updating it, and reopening both applications.',
+    percent: 100
+  })
+  isQuittingForHandoff = true
+  setTimeout(() => app.quit(), 1200)
+  return { ok: true, handedOff: true, message: 'Papers update helper started.' }
+})
+
 ipcMain.handle('hermes:updates:branch:get', async () => readDesktopUpdateConfig())
 
 ipcMain.handle('hermes:updates:branch:set', async (_event, name) => {
diff --git a/apps/desktop/electron/preload.ts b/apps/desktop/electron/preload.ts
index 7e5526c00e..32f137a242 100644
--- a/apps/desktop/electron/preload.ts
+++ b/apps/desktop/electron/preload.ts
@@ -492,6 +492,12 @@ contextBridge.exposeInMainWorld('hermesDesktop', {
       return () => ipcRenderer.removeListener('hermes:updates:progress', listener)
     }
   },
+  papers: process.env.HERMES_DESKTOP_PAPERS_DOCK_URL
+    ? {
+        managed: true,
+        update: () => ipcRenderer.invoke('hermes:papers:update')
+      }
+    : undefined,
   themes: {
     fetchMarketplace: id => ipcRenderer.invoke('hermes:vscode-theme:fetch', id),
     searchMarketplace: query => ipcRenderer.invoke('hermes:vscode-theme:search', query)
diff --git a/apps/desktop/src/global.d.ts b/apps/desktop/src/global.d.ts
index bb09b46c48..460f25bfa0 100644
--- a/apps/desktop/src/global.d.ts
+++ b/apps/desktop/src/global.d.ts
@@ -486,6 +486,10 @@ declare global {
         setBranch: (name: string) => Promise<{ branch: string }>
         onProgress: (callback: (payload: DesktopUpdateProgress) => void) => () => void
       }
+      papers?: {
+        managed: true
+        update: () => Promise<DesktopUpdateApplyResult>
+      }
       uninstall: {
         summary: () => Promise<DesktopUninstallSummary>
         run: (mode: DesktopUninstallMode) => Promise<DesktopUninstallResult>
diff --git a/apps/desktop/src/store/updates.ts b/apps/desktop/src/store/updates.ts
index f6e977f975..df579bca33 100644
--- a/apps/desktop/src/store/updates.ts
+++ b/apps/desktop/src/store/updates.ts
@@ -645,6 +645,27 @@ async function runBackendUpdate(): Promise<DesktopUpdateApplyResult> {
   })
 
   try {
+    const papers = window.hermesDesktop?.papers
+    if (papers?.managed) {
+      $backendUpdateApply.set({
+        ...IDLE,
+        applying: true,
+        stage: 'restart',
+        message: 'Papers is closing Hermes, updating it, and reopening both applications.'
+      })
+      const result = await papers.update()
+      if (!result.ok) {
+        $backendUpdateApply.set({
+          ...IDLE,
+          applying: false,
+          stage: 'error',
+          error: result.error ?? 'papers-update-failed',
+          message: result.message ?? 'Papers could not start the Hermes updater.'
+        })
+      }
+      return result
+    }
+
     const previousStatus = $backendUpdateStatus.get()
     const requestedTargetSha = previousStatus?.commits?.at(0)?.sha
 
