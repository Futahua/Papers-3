node_modules/
out/
dist/
release/
coverage/
*.log
.DS_Store
Thumbs.db
# Local-only workspaces (never committed)
.local/
.papers-dev-data/
# Test artifacts
test-results/
playwright-report/
# Environment files
.env
.env.*
out.txt
err.txt
# Vite writes a temporary config bundle into the repo root while loading the TS
# config. It is deleted immediately, but it exists during the build — long enough
# to make a clean tree look dirty to anything inspecting git status mid-build.
electron.vite.config.*.mjs
# Python bytecode from the companion connector
__pycache__/
